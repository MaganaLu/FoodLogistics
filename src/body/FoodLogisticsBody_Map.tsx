import React from 'react';
import { View } from 'react-native';
import { FontAwesome } from '@expo/vector-icons';

export default class Map extends React.Component {
    render() {
        return (
            <FontAwesome name='map-o' size = {50}/>
        )
    }
}